function compareOnlyAlphabet(str1, str2) {
  // TODO: 여기에 코드를 작성합니다.
  // 대문자 소문자로 변경해서
  // 같으면 true 다르면 false

  if (str1.toLowerCase() === str2.toLowerCase()) {
    return true;
  } else return false;
}
